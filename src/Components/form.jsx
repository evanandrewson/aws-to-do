import React from 'react';

const Form = () => {

    const handleSubmit = () => {};
    
    return (
        <form onSubmit={handleSubmit}>
            <input type="text" name="todo"></input>
            <button></button>
        </form>
    )
};

export default Form;
